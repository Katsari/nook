{
  "schemaVersion": 1,
  "id": "io.github.katsari.nook",
  "name": "Nook",
  "version": "1.1.1",
  "author": "Katsari",
  "description": "A bar drawer for when too many plugins have cluttered your bar. Collapse the ones you rarely touch into a tray that opens below the bar rather than along it, so nothing on the bar gets covered or moved. Drag widgets in and out.",
  "kinds": [
    "bar-widget"
  ],
  "entryPoints": {
    "barWidget": "BarWidget.qml"
  },
  "barWidget": {
    "displayName": "Nook",
    "description": "A bar drawer for when too many plugins have cluttered your bar. Collapse the ones you rarely touch into a tray that opens below the bar rather than along it, so nothing on the bar gets covered or moved. Drag widgets in and out.",
    "category": "Layout",
    "allowMultiple": false,
    "defaultSection": "right",
    "defaults": {
      "items": [],
      "trigger": "hover",
      "duration": 180
    },
    "schema": [
      {
        "key": "trigger",
        "type": "enum",
        "label": "Open on",
        "options": [
          "hover",
          "click"
        ],
        "defaultValue": "hover"
      },
      {
        "key": "duration",
        "type": "integer",
        "label": "Reveal duration (ms)",
        "min": 0,
        "max": 1000,
        "defaultValue": 180
      }
    ]
  },
  "license": "MIT"
}
